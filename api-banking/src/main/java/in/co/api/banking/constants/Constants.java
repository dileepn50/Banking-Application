package in.co.api.banking.constants;

public class Constants {
    public enum TransactionType {
        WITHDRAW,
        DEPOSIT
    }

    public enum AccountType {
        SAVING,
        CURRENT
    }
}
